node -v

npm install node-fetch
sudo apt-get install -y nodejs

npm install dotenv

GITHUB_TOKEN=your_github_token_here
USERNAME=your_github_username
FILENAME=settings.js
REPO_NAMES=repo1,repo2,repo3

When generating the token, you might see something like this:

    [x] repo (This scope grants full control of private repositories. It's necessary if you need access to private repositories to fetch the settings.js files.)

        [ ] repo:status (Access commit status)

        [ ] repo_deployment (Access deployment status)

        [x] public_repo (Access public repositories)

        [ ] repo:invite (Manage repository invitations)

    [x] read:org (If your repositories are within an organization, this scope allows you to read the organization’s data.)

chmod +x /path/to/your/script.js

cd /path/to/your/
node datadump.js

crontab -e
0 * * * * /usr/bin/node /path/to/your/datadump.js
0 * * * * /usr/bin/node /path/to/your/datadump.js >> /path/to/your/logfile.log 2>&1







